import { NodePosition, VNode } from "./types";
export declare class VDomArray extends Array {
}
export declare function buildTree(vnode: VNode, anchor: HTMLElement, position?: NodePosition, staticNodes?: HTMLElement[]): void;
/**
 * This function assumes that oldvnode has been patched first (and so, has valid
 * html or text elements)
 *
 * Note that it assumes that vnode and target are the same (same key/type/...)
 * It mutates vnode in place
 */
export declare function patch(vnode: VNode, target: VNode, staticNodes?: HTMLElement[]): void;
